# Where the FastAPI backend lives. Must be an origin listed in the backend's
# CORS_ORIGINS, and every request from here carries the session cookie, so it
# also has to be the host the cookie was set for.
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_API_PREFIX=/api/v1
